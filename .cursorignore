# --- 核心忽略项 ---
# 依赖库：这是产生噪音最大的地方，必须忽略
node_modules/

# Git 版本控制文件
.git/


# --- 构建与输出 ---
# 打包生成的代码，AI 不需要读，它只需要读源码
dist/
build/
out/

# --- 锁定文件 ---
# 这些文件通常有几千行，全是版本号，没有逻辑，会占满索引空间
package-lock.json
pnpm-lock.yaml
